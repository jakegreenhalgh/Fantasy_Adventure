package Characters;

import Items.ArmourType;
import Items.WeaponType;

public class Knight extends Fighter {
    public Knight(String name, int health, WeaponType weaponType, ArmourType armourType) {
        super(name, health, weaponType, armourType);
    }
}
